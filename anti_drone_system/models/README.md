# Models Directory

Place your custom trained YOLO weights (`best.pt`) in this folder.

If `best.pt` is not found, the detector module will automatically fall back to the standard pre-trained `yolov8n.pt` for local simulation and testing.
